# Ignore all files inside these folders, but keep the folder itself
uploads/cvs/*
uploads/events/*
uploads/newsletter/*
uploads/photos/*
uploads/payment_slips/*
# But do NOT ignore the folders themselves
!uploads/cvs/
!uploads/events/
!uploads/newsletter/
!uploads/photos/
uploads/payment_slips/
